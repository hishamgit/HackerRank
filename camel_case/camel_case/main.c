#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

int main()
{
    int i;
    char c[];
    scanf("%s",&c);
    if (c[0]=='S')
    {
      if(c[2]==M)
      {
         for(i=4;i++;i=!'\n')
         {
             if( isupper(c[i]))
             {
                 break;
             }
         }
         i=k;

         c=tolower(c);
      }
      else if(c[2]==V)
      {

      }
      else()
      {

      }
    }
    if(c[0]=='C')
    {

    }
    return 0;
}
